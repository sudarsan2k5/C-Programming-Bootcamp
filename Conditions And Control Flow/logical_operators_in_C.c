#include <stdio.h>

int main(){
    int money;
    int grade;
    scanf("%d %d", &money, &grade) return 0;

    if(money < 50 && grade > 90)

    if(money < 50 ||grade > 90)

    if(!(grade > 80))
    printf("Good Job!\n");
    retuen 0;
}
